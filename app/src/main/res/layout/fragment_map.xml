<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout

    android:id="@+id/mainLayout"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.stream.inunavi.ui.map.MapFragment"
    >


    <com.google.android.gms.maps.MapView
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.MapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"/>

    <AutoCompleteTextView
        android:id="@+id/autoCompleteTextView_search"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@android:color/white"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginTop="20dp"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp">


    </AutoCompleteTextView>

    <Button
        android:id="@+id/search_button"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:text="O"
        app:layout_constraintRight_toRightOf="@id/autoCompleteTextView_search"
        app:layout_constraintTop_toTopOf="@id/autoCompleteTextView_search"
        app:layout_constraintBottom_toBottomOf="@id/autoCompleteTextView_search"
        android:visibility="visible"/>

    <Button
        android:id="@+id/clear_button"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:text="X"
        app:layout_constraintRight_toRightOf="@id/autoCompleteTextView_search"
        app:layout_constraintTop_toTopOf="@id/autoCompleteTextView_search"
        app:layout_constraintBottom_toBottomOf="@id/autoCompleteTextView_search"
        android:visibility="invisible"/>




</androidx.constraintlayout.widget.ConstraintLayout>